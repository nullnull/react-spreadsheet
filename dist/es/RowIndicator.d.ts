import * as React from "react";
export declare type Props = {
    row: number;
    label?: React.ReactNode | null;
};
export declare const RowIndicator: ({ row, label }: Props) => React.ReactElement;
export default RowIndicator;
